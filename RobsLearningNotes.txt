Cameras
-CinemachineCamera
--Controls the camera configuration
--FoV, target to follow, "look at" etc.
-CinemachineBrain
--Determines which camera should be active and transitions between
--Must be attached to "camera" ?? (main camera?)
-CinemachineFollow
-CinemachineRotationComposer
Input System
- Polling (checking every frame, less common these days)
- Event-driven (recieving callback when input recieved, more common now)
- This course will use polling.
Physics Colliders
- Edit / project settings / Physics / settings
- Gives layerwise collider matrix.
- Physics Materials: friction comes about as a combination of both collider physics materials! Can have zero friction from one and non-zero from the other.
AI Navigation Toolbox
- Nav mesh areas: different cost multipliers for different areas (different for different agent types)
- Render meshes vs physics colliders
- Nav surface volumes
- Nav quality None (when gathering resources) and High when navigating
- Nav priority: can push others out of the way (lower priority number is more powerful)
- NavMeshModifiers and NavMeshModifiersVolumes: used to manipulate Areas when baking NavMesh
- NavMeshObstacle: allows nav mesh to be effectively modifified at runtime through "carvingf", without requiring re-bake
- Long story short: Use NavMeshModifierVolumes to change bake, and use NavMeshObstacle to change at runtime.
- "Unit Dancing" problem: multiple nav agents with the same priority. Set new nav positions based on number of selected units 
Decal URP Projector
- Used in this project for selection
- projects onto specific meshes that we can choose
Event Bus
- Loose coupling / sollution to interclass comms (Bus.cs example)
- "public delegate void Event(T args);" // "Define a type 'Event' that represents any function returning void and taking one parameter of type T."
- "public static event Event OnEvent;" // Define parameter of Event type that can only be invoked by Bus class ("event" keyword)
- "public static void Raise(T evt) => OnEvent?.Invoke(evt);" // expression bodied function
HashSet
- high performace set (a set is collection w/o duplicates)
- order isn't kept track of
Scriptable Objects
- classes used to store data / configuration and may contain functions to execute code related to that data.
- used to store data at build time.
Nice reminder of inheritance in c#
- protected virtual (allow children to overwrite function), protected override (force override of virtual). Nice example in AbstractUnit
- To run parent Method, use base.Method(). Nice example in AbstractUnit
Design Patterns
- "Command" pattern and command contexts
- Structs are much quicker to instantiate, apparently
Handling UI
- The whole thing is a cluster f.
- See mermaid diagrams in BaseCommand and PlayerInput for some nightmarish design patterns
- apparently wasMouseDownOnUI = EventSystem.current.IsPointerOverGameObject() detects mouse over UI?
Queues
- Like lists, but can Enqueue, Dequeue and Peek next value
Behaviour Trees
- Control Flow Nodes (how logic flows)
- Action nodes (defines logic of actions that will be performed)
- To use on gameObjects, Requires gameObjects have BehaviorAgent
- Make graphs with right click on project window -> create -> behaviour -> behavior graph
- Cool creation wizard in the tree editor... generates script skeletons for you
- Dont forget the OnEnd feature. Useful for handling aborts and other stuff.
- Behaviour Events / Event Channel Type (delegates)
Comparators and System.Linq
- Really cool way to filter and order colliders. Look at ChooseClosestGatherableSupply and ClosestColliderComparer
For performance, Avoid using Update()
- In complex games with lots of game objects, try not to use MonoBehaviour.Update()
- Try to use the built in components... much more optimised
- Can also use Entity Component System (ECS) 
Font Issue: uncontrolled changes.
- Chaning Atlas Population mode to dynamic on font will help stop uncontrolled changes. I was uncontrollably spamming the repo with 2 mb changes.
Object initialisers
- Example in BuildingRestrictionSO.cs
Performative physics checking
- note "non alloc" versions of physics sphere overlap and the like. Example in BuildingRestrictionSO.CanPlace
Neat switch approach using enums.
- Example in BuildingRestrictionSO.CanPlace
Event System Triggers (Event Triggers)
- Attatch EventTrigger to UI objects
- Key example use is on UI objects
- look at tooltips (UIActionButton, OnPointerEnter, OnPointerExit)
nameof function
- oh my god. Solves issue of hard coding variable names as strings directly into code.